export default function AdminTable({title,headers,rows}:{title:string;headers:string[];rows:string[][]}){return <div className="card overflow-auto"><h3 className="mb-2 font-semibold">{title}</h3><table className="w-full text-sm"><thead><tr>{headers.map(h=><th key={h} className="border-b p-2 text-left">{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((c,j)=><td key={j} className="border-b p-2">{c}</td>)}</tr>)}</tbody></table></div>}
